# Descriptor for one project's record-vs-full-text comparison. See ../WORKFLOW.md.
# DEVIATION: this baseline annotates from study_abstract, not study_fulltext, so its
# annotation stage cannot see which document an arm read. Not fixable on this host --
# its retrieval sources point at another machine. Its model ids are also bare slugs.
project:      vbm_of_substance_use
meta_pmid:    "36115222"
baseline_run: v2
pondie_run:   sud
